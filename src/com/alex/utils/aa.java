package com.alex.utils;

public class aa {

	public aa() {
		// TODO Auto-generated constructor stub
//		String processes = Terminal.sendShortCommandLine(Constants.ADB + " -s " + device.getSerialnum() + " shell ps");
//		Matcher matcher = Pattern.compile("^\\w+\\s+(\\d+)\\s.*" + PROCESS_NAME + "$", Pattern.MULTILINE).matcher(processes);
//		if(matcher.find()) {
//			String pid = matcher.group(1);
//			String result = Terminal.sendShortCommandLine(Constants.ADB + " -s " + device.getSerialnum() + " shell \"kill -9 " + pid + "\"");
//			logger.info("device id:" + device.getId() + " Serialnum:" + device.getSerialnum() + " Kill suserver pid=" + pid + ":" + result);
//		}
//		
//		processes = Terminal.sendShortCommandLine(Constants.ADB + " -s " + device.getSerialnum() + " shell ps");
//		Matcher subMatcher = Pattern.compile("^\\w+\\s+(\\d+)\\s.*" + SUB_PROCESS_NAME + "$", Pattern.MULTILINE).matcher(processes);
//		while(subMatcher.find()) {
//			String pid = subMatcher.group(1);
//			String result = Terminal.sendShortCommandLine(Constants.ADB + " -s " + device.getSerialnum() + " shell \"kill -9 " + pid + "\"");
//			logger.info("device id:" + device.getId() + " Serialnum:" + device.getSerialnum() + " Kill suserver pid=" + pid + ":" + result);
//		}
	}
	

}
